# HWR-GRASS-001 R3 — 정적 잔디·흙길·전체 마을 구도

갱신: 2026-09-14 UTC
상태: 사용자 시각 피드백에 따른 **다음 수정 지시**. R3 구현·게임 반영 완료 보고가 아니다.
대상: 기존 풀 작업 담당 Fable / Claude Code 로컬 세션.

## 지금 적용할 지시

**[VILLAGE_GROUND_IMPLEMENTATION.md](docs/VILLAGE_GROUND_IMPLEMENTATION.md)를 읽고 R3를 진행한다.** 이전 결과를 보존하면서 넓은 지면의 표현을 바꾼다.

현재 결과는 타일마다 17,156삼각형의 높이장·흙 벽·앞뒤 카드다. 단독 감면/부하는 개선됐으나 사용자는 반복 배치의 외관과 바람을 거절했다. 다음 구현은 아래로 전환한다.

1. 기존 불투명 바닥 메시 위에 낮은 대비의 잔디·흙 텍스처를 연속 좌표로 표시한다.
2. 실제 길/입구/완공된 도로 정보를 지면 재질의 혼합 마스크로 연결한다.
3. 작은 개별 잎 묶음 3종을 가장자리에 드문드문 배치하고 이동·건설 공간을 비운다.
4. 풀의 시간 의존 정점 이동과 밝기 진동을 제거한다.
5. 실제 측면 카메라 한 화면에서 길·나무·우물·밭·정령을 함께 검수한 후 확장한다.

그림의 굽은 길·강 위치·새 장식을 그대로 복제하지 않는다. 현재 템플릿·저장·건설 조건과 일치하는 공간에서 색감과 배치 밀도를 맞춘다. **텍스처와 작은 풀 묶음 제작이 필요하며 기존 tint 조정만으로 완료하지 않는다.**

## 이전 지시 중 폐기·유지 사항

R3는 이 경로의 이전 R1/R2보다 우선한다. 과거 내용은 Git 이력에 보존돼 있다.

- 폐기: 전면을 촘촘한 원본 풀 타일로 덮는 방식, 타일당 LOD0 최소 10k면, 150/192개의 동일 타일을 강제하는 검사, 높이장/흙 벽/바람 마스크 의무, 바람 ON/OFF 영상과 세기 UI 요구.
- 유지: 사용자 원본 보존, 재현 가능한 제작, UV/텍스처 정합성, 게임용 개별 파일 25,000,000 bytes 이하, 리소스 공유, 실제 엔진 시각 검수와 로컬 GPU 성능 기록.
- 유지: 정령 디자인, 16×12 작은 마을, 직교 20°/크기 8.5 카메라, 부쉬 정돈 제거, 비옥토 제한 없는 밭 설치, 마을 바로 테스트.
- 유지: 전투·경제·저장·관개·주민 작업 규칙. 지면과 장식 표시를 바꾸는 작업이다.

## 작업 기준과 원본

검토 기준: `claude/hwr-grass-001`의 `80000e2df6b19f677169cfb258e13315d770006e`.
착수 시 최신 원격·미커밋 변경을 확인한다. main만 받아 기존 풀 작업을 새로 만들거나 완료 결과를 삭제하지 않는다. `CLAUDE.md`와 `reports/README.md`를 따른다.

원본: `source_assets/environments/tiles/grass block 3d model.glb`
- 60,363,036 bytes
- SHA-256: `2f9003c54d01f66b76af992fb2eb5f48449dfeac78a16e9c95b9c207beee7363`
- 1,929,690삼각형, 1메시/1재질, 내부 BaseColor·metallic/roughness·normal, 리깅 없음.

LFS 실물과 glTF 헤더를 확인한다. 사용자가 새 원본을 올렸으면 출처를 기록하고 원본을 덮어쓰지 않는다. 원본은 game 밖, 실제 게임용 출력만 game 안에 둔다. 새 UV에는 그에 맞는 재베이크/새 텍스처가 필요하다.

## 환경 담당과의 분리

[HWR-ENV-001](TO_CLAUDE_ENVIRONMENT.md)은 별도 담당의 나무·우물 등 환경 모델 작업이다. 풀 담당은 그 모델을 재제작하지 않는다. 별도 worktree/clone을 유지하고 공통 파일은 변경 함수를 기록해 통합한다.

- 풀 소유 영역: `game/assets/environment/grass/`, `tools/environment/grass/`, 풀 검토 장면/보고.
- 공유 연결: `village_stage_3d.gd`의 `_build_terrain()`, `sync_buildings()` 등. 환경 모델 factory·재질 교체를 보존한다.
- 환경 모델이 미완료이면 지면 검사까지만 완료라고 기록한다. 통합된 실제 마을 캡처가 있어야 합본 미술 검수를 완료했다고 할 수 있다.
- 과거 M4 192타일 약 5.3ms는 풀 단독 보고 수치다. 전체 게임 성능으로 재사용하지 않는다.

제출물·검수·파일 연결 상세는 [R3 구현서](docs/VILLAGE_GROUND_IMPLEMENTATION.md)에 있다. 새 결과는 `reports/HWR-GRASS-001_R3.md`에 기록한다.
