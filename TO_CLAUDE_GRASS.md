# HWR-GRASS-001 — Tripo 풀밭 경량화·타일 정리·바람 미리보기

작성: 2026-09-14 UTC
상태: 사용자 확인상 별도 담당이 구현 중. 작업 브랜치 `claude/hwr-grass-001` (원격 확인 `5544f88aa4c17a288f655fffd348ea19e90dce42`). 본 갱신은 main 병합·담당 분리 안내이며 기존 풀의 수치/범위/검수 기준은 유지한다. 미커밋 로컬 진척과 실물 성능은 이 문서에서 추측하지 않는다.

## R2 — 사용자 시각 검토 반영: 풀 고정 및 전체 마을 구도 우선

2026-09-14 후속 요청. 사용자는 완성본의 192타일 조합이 조잡하고 바람이 어색하다고 판단했다. **아래 R2가 이 문서의 기존 바람 구현·ON/OFF 영상 요구보다 우선한다.** 기존 결과는 보존하고 변경분만 적용한다.

- 이번 버전에서 풀은 고정한다. 시간에 따른 정점 변형·gust·wind sheen/색 밝기 진동을 끈다. 단순히 화면의 한 프레임을 멈추거나 게임 전체 TIME을 고정하지 않는다. 기존 셰이더가 남더라도 런타임 풀 표시에서 변형과 광택 진동이 발생하지 않아야 한다. 정령의 작업 동작이나 다른 게임 애니메이션은 유지한다.
- 완료 기준에서 바람 ON/OFF 비교·바람 영상 제출을 제외한다. 기존 바람 코드를 개선하는 데 추가 시간을 쓰지 않는다. 새 기본값과 실제 마을의 정지 잔디를 확인한다.
- 첨부된 192타일 스크린샷은 밝은 황록색, 촘촘한 고대비 조각, 균일한 밀도가 반복돼 넓은 면에서 시각적으로 시끄럽다. 화면만으로 텍스처·셰이더·조명 중 단일 원인을 단정하지 말고 각각 분리해 검사한다.
- 바닥은 차분한 세이지/올리브 계열의 낮고 연속된 잔디로 정리한다. 큰 색/명암 변화는 넓고 부드럽게, 잎 실루엣은 필요한 곳에 남긴다. 네모 단위 랜덤 색 패치나 반복된 밝은 점무늬로 바꾸지 않는다.
- 실제 마을에서는 흙길·입구·작업 공간에 여백을 두고, 선명한 풀/클로버 묶음은 길 가장자리·나무 밑·울타리 주변에 제한적으로 배치한다. 기존 논리 타일·길찾기·밭 조건을 바꾸지 않고 표시 밀도만 설계한다.
- 192개는 접합/부하 스트레스 검사다. 실제 마을 모든 칸을 같은 고밀도 풀로 채우라는 뜻이 아니다. 단독 타일 확대보다 **같은 낮은 측면 카메라에서 풀+길+나무+건물+정령이 함께 보이는 화면**을 우선 검수한다.
- 먼저 작은 대표 구역 한 화면으로 색·밀도·축척·이음새를 검수한 뒤 확장한다. 색/밀도 문제를 숨기려고 카메라를 탑다운으로 바꾸거나 과한 블룸/피사계심도를 넣지 않는다.
- 대화에서 제공하는 새 전체 마을 이미지는 목표 콘셉트이며 실제 엔진 실행 증거가 아니다. 원본 텍스처와 경량화 결과를 보관하고, 구현 화면과 같은 구도로 비교한다. UI/카메라/공통 코드를 수정한다면 환경 담당과의 worktree·파일 분리 규칙을 유지한다.

## 담당 AI에게

사용자가 만든 풀밭의 따뜻한 황록색·올리브색, 짧은 풀과 작은 잎의 겹침을 보존하면서 마을에 반복 배치할 수 있는 게임용 에셋으로 만들어라. 단순한 초록 판으로 바꾸거나 흙 받침 전체를 흔들지 않는다. 원본은 보존하고, 재생성 가능한 변환 절차와 실제 Godot 비교 결과를 제출한다.

이 문서는 독립 작업이다. 현재 TO_CLAUDE.md는 풀과 환경 9종의 작업 라우터다. 기존 풀 담당은 환경 작업으로 전환하거나 HWR-006 전체를 다시 구현하지 않는다. CLAUDE.md와 reports/README.md를 읽고, 기존 작업 브랜치와 미커밋 작업을 보존해 이어 간다.

게임 구현은 이제 main `22a21cf974479cb6fc213fe9c0120e31520db6d1`에 PR #2로 병합됐다. 예전 `claude/magical-bohr-2eljuj`를 새 기준으로 찾지 않는다. 시작할 때 최신 원격과 사용자 변경을 확인하고 실제 기준 커밋을 기록한다. 승인된 정령, 곧게 선 새 나무 콘셉트, 작은 마을, 측면 카메라, 부쉬 정돈 제거, 자유로운 밭 설치를 보존한다.

## 환경 작업과 병행할 때

- 풀 코드·모델·검토/보고는 본 담당, 나무·우물 등 환경 9종은 [HWR-ENV-001](TO_CLAUDE_ENVIRONMENT.md) 담당이다. 서로 다른 worktree/clone을 쓰고 같은 폴더에서 브랜치를 전환하지 않는다.
- `game/assets/environment/grass/`, `tools/environment/grass/`는 풀 담당 영역이다. 공유 `village_stage_3d.gd`·`garden_assets.gd`·공통 시험 변경은 함수 단위로 보고하고 통합 시 양쪽 표시를 보존한다.
- 현재 풀 브랜치에 환경 원본 5개가 같이 들어 있다. 환경 담당이 원본 경로만 가져오는 것은 허용하되 풀 구현 커밋 전체를 복제하거나 사용자의 중간 파일을 삭제하게 하지 않는다.
- 이후 재생성 가능한 대형 베이크 중간 배열을 추가 커밋하지 않도록 자신의 출력/cache 관리 방식을 점검한다. 이미 원격에 있는 작업물을 여기서 강제 삭제하거나 이력을 재작성하지 않는다.
- 실제 Mac 성능을 잴 때 환경 담당의 무거운 베이크/렌더와 동시 실행하지 않는다. GPU 장치 로그·renderer를 남긴다. llvmpipe의 시간은 소프트웨어 측정으로 구분하되 draw-call 작업량 비교까지 무의미하다고 버리지 않는다.

## 1. 원본 찾기와 보존

사용자가 실제로 업로드한 경로 (이 경로를 우선 사용):
`source_assets/environments/tiles/grass block 3d model.glb`

원본은 현재 main과 풀 작업 브랜치에 모두 등록돼 있다. 실제 작업 브랜치의 LFS 실물을 확인한다. 파일명에 공백이 있으므로 셸에서는 전체 경로를 인용한다. 문서의 옛 예시 경로로 파일을 옮기거나 중복 업로드할 필요는 없다.

등록된 LFS 포인터의 size는 60,363,036 bytes, SHA-256은 아래 첨부 원본 검사값과 일치한다. 실제 다운로드 후에도 같은 크기·해시와 GLB 헤더를 확인한다.

같이 업로드된 나무: `source_assets/environments/objects/tree 3d model.glb`. LFS 포인터 기준 57,545,824 bytes, SHA-256 `aec969e140710e5b438bd984814b84b23643a73abeda268aa4812d556cd6d36d`. 나무의 실제 메시·면수는 아직 검사하지 않았다. 이 문서의 풀 전용 수치와 감면 비율을 그대로 적용하지 않는다. 나무 원본도 보존한다.

제작용 원본은 `game/` 밖에 둔다. Godot이 60MB 원본까지 매번 가져오거나 게임 배포에 포함하지 않도록 하기 위함이다. 이후 사용자가 경로를 변경했으면 최신 실제 경로를 기록하고 사용한다. 임의 파일로 대체하지 않는다.

LFS 저장소는 `git lfs install` 후 원본이 있는 브랜치에서 `git lfs pull`로 실물을 받는다. 100여 바이트의 LFS 포인터 텍스트를 GLB로 오인하지 말고, 헤더가 `glTF`인지 확인한다. 원본이 아직 없으면 나머지 코드·작업 절차를 준비하고 원본 부재만 보고한다. 원본 업로드 완료를 추측하지 않는다.

대화에 첨부된 실제 원본 검사값:

| 항목 | 측정값 |
|---|---|
| 첨부 파일명 | grass block 3d model.glb |
| 파일 크기 | 60,363,036 bytes |
| SHA-256 | `2f9003c54d01f66b76af992fb2eb5f48449dfeac78a16e9c95b9c207beee7363` |
| GLB 버전 | 2 |
| 메시 / 재질 / 노드 | 각각 1 |
| 정점 / 인덱스 | 1,053,084 / 5,789,070 |
| 삼각형 | 1,929,690 |
| 로컬 최소 좌표 | (-0.488739, 0, -0.490219) |
| 로컬 최대 좌표 | (0.488739, 0.145355, 0.490219) |
| 텍스처 | 내부 JPEG 3장: BaseColor, metallic/roughness, normal |
| 리깅 / 애니메이션 | 없음 |

해시가 다르면 사용자가 새로 내보낸 버전인지 파일·커밋 기록으로 확인하고 검사값을 갱신한다. 원본을 덮어쓰지 않는다.

## 2. 초기 예산과 품질 목표

아래 숫자는 프로젝트용 초기 예산이며 성능 측정 결과가 아니다. 이 예산에서 외형이 무너지면 비교 이미지를 제시하고 재구성 방식을 바꾼다. 숫자만 맞춘 결과를 완료로 처리하지 않는다.

| 단계 | 삼각형 수 목표 | 용도 |
|---|---:|---|
| LOD0 | 10,000~20,000, 최대 25,000 | 현재 마을 카메라와 가까운 검토 |
| LOD1 | 2,000~5,000 | 중간 거리 |
| LOD2 | 100~500 | 먼 거리의 풀 바닥, 필요한 낮은 굴곡만 유지 |
| 흙 바닥·옆면 | 수십~수백 수준 | 고정된 타일, 정밀 충돌 없음 |

- 최종 게임용 개별 파일은 **25,000,000 bytes 이하**, 대표 GLB는 가능하면 10MB 이하. 모델을 임의 바이트 청크로 나누지 않는다.
- 텍스처는 2K 공유 세트부터 시작한다. BaseColor는 색 공간, normal·ORM은 비색 데이터로 취급한다. 4K가 필요한 경우 동일 화면 비교와 메모리 차이로 이유를 남긴다.
- 거친 흙과 부드러운 풀의 무광 느낌, 작은 클로버 잎과 풀끝의 실루엣을 유지한다. 색 바램·번쩍임·검은 이음새·관통·새로 생긴 구멍을 허용하지 않는다.
- 원본의 제작용 텍스처는 남긴다. 새 UV를 만들면 원본 텍스처를 그대로 연결하지 말고 다시 베이크한다.
- 16개와 192개 배치의 실제 비용을 측정한다. 192개는 현재 16×12 마을 전체를 가정한 상한 검토이지 모든 칸에 풀을 깔라는 지시가 아니다. 물·길·건물 아래는 실제 배치 정책에 맞춰 제외한다.

## 3. 권장 작업 순서

### A. 먼저 원본을 관찰한다

1. GLB 구조, 텍스처 해상도, 경계·퇴화 면·뒤집힌 법선, 연결 덩어리와 흙/풀의 구분을 검사한다.
2. 동일 카메라·조명에서 정면, 위, 측면, 실제 마을의 낮은 측면 시점 기준 이미지를 확보한다. 원본 하나와 결과 하나를 비교하고, 193만 삼각형 원본을 192개 배치해 환경을 멈추게 하지 않는다.
3. 식물과 흙이 하나의 재질에 섞여 있다. 먼저 형상·UV·색으로 영역을 확인한 후 정점 가중치 또는 마스크를 만든다. Tripo 자동 Segmentation을 필수로 삼지 않는다.

### B. 표면과 실루엣을 나눠 경량화한다

1. Blender의 Collapse Decimate 또는 UV를 고려하는 QEM으로 40k → 20k → 10k 후보를 순차 비교한다. 시험 후보는 최종 게임 파일과 구분한다.
2. UV 경계·법선·표면 방향·풀끝 실루엣을 보존한다. 무조건 remesh/voxelize해서 잎을 하나의 덩어리로 만들지 않는다. 중복 정점 병합도 서로 다른 잎과 UV 경계를 무조건 붙이지 않는다.
3. 단순 감면으로 풀끝과 잎이 사라지면, 고정된 흙 바닥 + 낮은 풀 표면 + 실루엣을 담당할 몇 가지 풀잎 묶음으로 재구성한다. 흙 내부와 보이지 않는 면에 예산을 쓰지 않는다.
4. 큰 형태는 메시, 잔 잎맥·촘촘한 표면은 BaseColor/normal/ORM 베이크로 옮긴다. 새 UV와 여백을 만들고 고해상도 원본에서 투사한다. 베이크 cage/ray 거리로 옆 잎·흙 색이 엉뚱하게 찍히는 현상을 확인한다.
5. 알파 카드가 필요하면 불투명 얇은 잎 메시와 비교한다. 카드 사용 시 alpha scissor/hash와 mipmap 가장자리, 양면, 겹침에 의한 픽셀 비용을 확인한다. 투명 블렌딩 수백 겹으로 교체하는 것을 최적화라고 보고하지 않는다.
6. glTF 검증과 Godot 실제 가져오기로 UV·tangent·normal·재질을 확인한다. 원본 normal을 무조건 재사용하거나 모든 새 정점의 법선을 단순 최근접 복사하는 방식은 별도 시각 검증 없이 적용하지 않는다.
7. LOD0을 충분히 가볍게 만든 뒤 Godot 가져오기 LOD 또는 별도 LOD를 사용한다. 자동 LOD만 켜고 193만 삼각형 원본을 그대로 배포하지 않는다.

### C. 타일로 정리한다

- 원본은 정사각에 가깝지만 생성 결과의 모서리·윗면·밑면은 정확한 타일 규격이라고 가정하지 않는다.
- 에셋의 로컬 X/Z 외곽을 일정하게 맞추고 네 변의 접합 높이를 맞춘다. 흙 윗면을 기준 높이로 정의하고, 발밑과 맞출 피벗/오프셋/치수를 manifest에 기록한다.
- 바닥 메시를 서로 붙여도 흙 옆면이 위로 솟거나 빛이 새지 않아야 한다. 풀 오버행은 바닥의 틈을 가리는 대체 수단으로 쓰지 않는다.
- 2×2와 4×4로 연결해 직선 틈, 반복 무늬, z-fighting, 방향성 있는 베이크 그림자, 회전 시 단차를 검사한다. 반복이 눈에 띄면 공유 메시/텍스처와 회전·약한 색 변화부터 활용한다.
- 현재 표시 단위는 `VillageStage3D.CELL_W=2.0`, `CELL_D=0.9`로 깊이가 압축된다. 실제 최신 코드를 확인하고 기존 좌표/카메라 계약에 연결한다. 타일 때문에 논리 마을 크기나 길찾기 규칙을 바꾸지 않는다.
- 잔디 위에 풀이 자라난 평평한 땅으로 보이게 한다. 각 칸이 두꺼운 화분이나 독립된 공중 섬으로 보이면 실패다.

## 4. 바람: 최소 검토 장면에 포함

풀에는 캐릭터용 뼈대가 필요 없다. Godot vertex shader로 구현한다.

- 흙·타일 옆면·풀뿌리는 가중치 0, 풀끝으로 갈수록 1인 bend mask를 정점 색 또는 별도 채널에 저장한다. 실제 구분을 확인한 높이와 영역 마스크를 결합한다. 전체 높이 하나로 흙까지 흔들지 않는다.
- 공유 월드 좌표와 시간으로 바람 위상을 계산한다. 타일마다 같은 박자로 좌우 진동하는 모습과 경계에서 파동이 끊기는 모습을 피한다. 회전·스케일된 인스턴스에서 바람 방향이 올바르게 변환되는지 확인한다.
- 바람 세기 0일 때 정확한 원래 자세, 보통일 때 풀 높이의 약 5~10% 변위부터 시작한다. 바닥 블록은 움직이지 않는다.
- shader 변형을 포함하도록 AABB/extra_cull_margin을 확장해 화면 가장자리나 그림자에서 풀끝이 잘리지 않게 한다. 법선·그림자가 원래 위치에 고정되는 문제도 검사한다.
- 검토 장면에 바람 ON/OFF와 세기 조절을 제공하고 5~10초 실제 엔진 영상을 남긴다.
- 정령 주변 돌풍과 캐릭터가 밟는 반응은 후속 확장이다. 이번에는 균일한 바람과 타일 간 연결을 우선 완료한다.

## 5. 게임 연결과 성능 검증

기존 마을의 표시 계층에 연결 가능한 에셋과 단독 검토 장면을 제공한다. 원본·후보 전체를 런타임에 포함하지 않는다.

- 참고 파일: `game/scripts/village/village_stage_3d.gd`, `game/assets/garden_v1/garden_assets.gd`, `game/tests/run_village_entry_tests.gd`. 최신 브랜치에서 실제 존재 여부와 연결부를 확인한다.
- 텍스처·메시는 인스턴스 간 공유한다. 풀잎마다 Node를 만들지 않는다. MultiMesh 사용 시 지역 단위로 묶어 전체 마을이 한 번에 그려지는 문제를 피한다.
- 원본 1개 ↔ 결과 1개, 결과 16개와 192개, 바람 OFF ↔ ON을 비교한다.
- Godot 버전, OS/GPU, renderer(특히 현재 Compatibility), 해상도, 카메라, 실제 보이는 타일 수, 워밍업, 그림자 설정, 프레임 시간·FPS, draw calls, 표시 삼각형과 측정 가능한 메모리를 기록한다.
- 프로젝트 해상도에서 60FPS/16.7ms를 지향하되 확인하지 않은 Mac 성능을 보장하지 않는다. CPU/GPU 또는 다른 마을 에셋 병목을 구분한다.
- 실제 마을에 연결할 때는 클릭 설치·건설 미리보기·길·물·주민 발 위치와 가림을 확인한다. 마을 바로 테스트와 관련 기존 회귀 검사를 사용한다. 전투·경제·저장 데이터는 이번 작업에서 변경하지 않는다.
- GUI 확인이 불가능하면 정적 검사 결과와 미확인 항목을 분리하고 완료로 표시하지 않는다.

## 6. 제출물과 완료 기준

권장 게임 출력 폴더: `game/assets/environment/grass/`
제작용 원본: `source_assets/environments/tiles/`
재현 스크립트: `tools/environment/grass/` (필요 시 생성), 원본 검사값은 manifest와 보고서에 기록.

1. 최종 텍스처 포함 GLB, LOD 구성, 바람 재질, 에셋 장면과 실행 가능한 검토 장면.
2. 명령 한 번으로 재생성할 수 있는 스크립트 또는 재현 가능한 Blender 작업 파일·절차. 도구 버전과 입력/출력 경로를 명시한다.
3. manifest: 원본/결과 해시·용량·정점·삼각형·텍스처 크기·단위·피벗·고정 영역·LOD·라이선스 출처(사용자 제공 Tripo 모델).
4. 같은 카메라/조명에서의 전후 비교, 반복 타일 이미지, 실제 바람 영상. 콘셉트 이미지를 엔진 실행 증거로 쓰지 않는다.
5. `reports/HWR-GRASS-001.md`: 위 검사표, 실제 측정값, 실행법, 품질 손실, 남은 한계, 기준 커밋과 원격 반영 상태.
6. 원본은 Git LFS에 보존하고 최종 게임 파일만 실제 장면에서 참조한다. 파일 확장자를 바꾸거나 LFS 포인터만 전달해 업로드 성공으로 처리하지 않는다.
7. 사용자에게 수동 전달할 패키지는 각 파일 25MB 이하로 준비한다. 최종 GLB를 잘게 쪼개서 게임 실행 때 다시 합치게 만들지 않는다.

완료 판단은 파일이 작아졌는지뿐 아니라 **실제 마을 거리에서 원안의 풀 질감이 남는지, 반복 경계가 안 보이는지, 바람에도 흙이 고정되는지, 반복 배치 성능이 개선됐는지**로 한다.

## 참고 문서

- [Godot Spatial shaders](https://docs.godotengine.org/en/stable/tutorials/shaders/shader_reference/spatial_shader.html): vertex 처리와 좌표/시간 입력.
- [Godot Mesh LOD](https://docs.godotengine.org/en/stable/tutorials/3d/mesh_lod.html): 자동 LOD, 화면 기준 선택, MultiMesh 주의점.
- [Godot MultiMesh](https://docs.godotengine.org/en/stable/tutorials/performance/using_multimesh.html): 반복 표시와 지역별 묶음.
