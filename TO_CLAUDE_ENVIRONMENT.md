# HWR-ENV-001 — 나무·마을 오브젝트 9종 경량화와 게임 적용

작성: 2026-09-14 UTC
대상: Fable / Claude Code 로컬 세션. 사용자 요청은 현재 업로드된 풀 외 모델을 게임용으로 다듬어 실제 마을에 적용하는 것이다.
상태: 지시 발행. 이 문서는 모델 최적화·게임 적용·성능 검증 완료 보고가 아니다.

## 1. 이번 작업과 시작 기준

- 진행 중인 **HWR-GRASS-001은 다른 작업**이다. 풀 제작을 다시 시작하거나 해당 작업 폴더를 수정하지 않는다.
- 현재 main `22a21cf974479cb6fc213fe9c0120e31520db6d1`에 게임 구현과 PR #2가 병합됐다. 예전 `claude/magical-bohr-2eljuj`를 찾거나 HWR-006 전체를 재구현하지 않는다.
- 현재 풀 브랜치: `claude/hwr-grass-001`, 확인 커밋 `5544f88aa4c17a288f655fffd348ea19e90dce42`. 사용자의 미커밋 작업과 이후 커밋은 이 검사에 보이지 않을 수 있다.
- `CLAUDE.md`, `reports/README.md`, 본 문서, [에셋 목록](docs/ENVIRONMENT_ASSET_KIT.md), [입력 목록/해시](docs/ENVIRONMENT_SOURCE_INVENTORY.json)를 읽는다.
- 기존 정령, 전투, 저장·경제·일손·관개, 마을 바로 테스트, 작은 마을 크기와 낮은 측면 카메라, 부쉬 정돈 제거, 비옥토 제한 없는 밭 설치를 보존한다. 새 건설 기능·스토리를 추가하지 않는다.

### 별도 작업 폴더에서 시작

동시에 작업하는 두 세션이 한 폴더에서 브랜치를 바꾸면 미커밋 파일과 Godot import가 섞인다. 다른 브랜치 이름만으로는 충분하지 않다. 사용자의 기존 작업을 확인하고 별도 worktree 또는 별도 clone을 만든다.

예시: 기존 경로·브랜치가 없는 경우에만 아래 새 경로를 사용한다. 이미 있으면 상태를 확인해 이어 쓰거나 다른 고유 이름을 정한다. 기존 폴더를 지워서 맞추지 않는다.

```bash
git fetch origin
git worktree add -b fable/hwr-env-001 ../belthwararng-env origin/main
```

두 에이전트의 원본 처리·베이크를 같은 Mac에서 동시에 최대 부하로 실행하지 않는다. 새 세션은 자신의 worktree만 편집하고 각 프로세스의 출력·로그를 자기 폴더에 남긴다. 무관한 작업을 stash/reset/clean하거나 강제 push하지 않는다. 일반 커밋과 작업 브랜치 게시로 결과를 제출하고, 기존 저장소의 병합 절차를 따른다.

## 2. 실제 입력과 초기 예산

모든 경로는 `source_assets/environments/objects/` 아래다. 아래 크기는 **LFS 포인터의 원본 크기**, 삼각형 수는 **앞으로 만들 결과의 초기 목표**다. 원본 면수를 추정한 표가 아니다.

| 실제 파일명 | 기존 표시 ID | 원본 위치 | 원본 크기 | LOD0 삼각형 초기 목표 |
|---|---|---|---:|---|
| `fence.glb` | fence | 풀 브랜치에만 있음 | 57.29 MB | 500–2,000 / 최대 3,000 |
| `greenhouse.glb` | greenhouse | 풀 브랜치에만 있음 | 52.86 MB | 10,000–25,000 / 최대 30,000 |
| `rock.glb` | rock | main | 60.10 MB | 1,000–3,000 / 최대 5,000 |
| `signpost.glb` | cloud_sign | 풀 브랜치에만 있음 | 57.98 MB | 500–2,000 / 최대 3,000 |
| `stone well.glb` | well | main | 58.89 MB | 8,000–15,000 / 최대 20,000 |
| `stone workbench.glb` | quarry | 풀 브랜치에만 있음 | 57.61 MB | 6,000–15,000 / 최대 20,000 |
| `tree 3d model.glb` | tree / flower_tree | main | 57.55 MB | 20,000–35,000 / 최대 40,000 |
| `warehouse.glb` | shed | main | 58.20 MB | 10,000–25,000 / 최대 30,000 |
| `wood workbench.glb` | lumber | 풀 브랜치에만 있음 | 57.58 MB | 6,000–15,000 / 최대 20,000 |

9개 원본 합계는 약 518 MB다. 모두 한 번에 Blender에 올리거나 전체를 고해상도로 반복 배치하지 않는다. 원본 하나씩 검사·변환·기록하고 다음으로 넘어간다.
LOD1은 LOD0의 약 30–50%, LOD2는 약 10–20%부터 비교하되, 작은 소품은 유효한 화면 차이가 없으면 불필요한 별도 LOD를 생략할 수 있다. 나무 잎과 지붕 외곽이 사라지는 결과를 숫자가 맞는다는 이유로 채택하지 않는다.

최종 개별 게임 파일은 25,000,000 bytes 이하, 대표 GLB는 가능하면 10 MB 이하. 텍스처는 1K–2K부터, 큰 나무·건물에서 4K가 필요하면 실제 마을 거리 비교로 이유를 기록한다. 최종 리소스 용량, 표시 삼각형, 재질/표면 수, 텍스처 메모리를 함께 관리한다. 큰 원본 자체에 25 MB 제한을 억지로 적용하거나 바이트 청크로 잘라 게임에서 합치게 만들지 않는다.

### 풀 브랜치에만 있는 원본 5개 가져오기

`5544f88aa4c17a288f655fffd348ea19e90dce42`는 원본 추가와 풀 셰이더·제작 도구·중간 산출물이 섞인 커밋이다. 이 커밋을 통째로 cherry-pick하거나 풀 브랜치를 이번 작업에 병합하지 않는다. 확인 시 `tools/environment/grass/build/`에는 약 226 MB의 중간 파일이 있다. 이 작업에서 복사하거나, 풀 담당자의 파일을 삭제·정리하지 않는다.

최신 main에 이미 같은 원본이 들어왔는지 먼저 비교한다. 누락된 경우에만 **새 환경 worktree**에 아래 경로들을 원본 추가 커밋에서 선택적으로 복원한다.

- `source_assets/environments/objects/fence.glb`
- `source_assets/environments/objects/greenhouse.glb`
- `source_assets/environments/objects/signpost.glb`
- `source_assets/environments/objects/stone workbench.glb`
- `source_assets/environments/objects/wood workbench.glb`

예: 누락된 파일에 대해 `git restore --source=5544f88aa4c17a288f655fffd348ea19e90dce42 --staged --worktree -- '<정확한 파일 경로>'`. 최신 같은 경로의 다른 해시를 발견하면 오래된 것으로 덮어쓰지 말고 변경 내용을 확인한다. 원본만 옮기는 커밋을 별도로 남겨 이후 풀 브랜치와 병합할 때 출처를 추적할 수 있게 한다.

LFS로 해당 원본 커밋의 objects 경로 실물을 fetch/checkout하고 각 파일의 SHA-256·실제 크기·`glTF` 헤더를 입력 목록과 대조한다. 원격 포인터가 보인다는 이유만으로 실물 다운로드 성공을 보고하지 않는다. 파일명 공백을 인용하고 원본 이름을 임의로 바꾸지 않는다.

## 3. 실행 환경과 단계별 진행

1. **로컬 확인:** cwd·worktree·브랜치·변경 파일·현재 main을 기록한다. Git LFS, Blender 또는 bpy, 사용 중인 Godot의 실제 버전·실행 경로를 확인한다. 특정 버전을 추측해 강제 설치/업그레이드하지 않는다.
2. **GPU 확인:** Godot을 일반 렌더링으로 실행하고 로그의 renderer와 장치 이름을 기록한다. 현재 프로젝트는 `gl_compatibility`, 1280×720이다. 물을 보기 좋게 하려고 전체 프로젝트를 Forward+로 바꾸지 않는다. headless 성공을 시각 검증으로 처리하지 않는다.
3. **입력 검사:** 9개 각각 원본 면수·재질·UV·텍스처 크기·노드 변환·경계·법선·구멍·피벗과 뒷면을 검사한다. 자동 Segmentation이나 전체 voxel remesh를 일괄 적용하지 않는다.
4. **시범 3종:** 바위 → 울타리 → 우물로 변환과 재질/미리보기/가림 처리까지 검증한다. 같은 원본을 계속 재생성하지 않도록 해시·설정별 결과를 재사용한다.
5. **나머지:** 나무 → 표지판 → 창고·온실·목재/석재 작업대를 한 개씩 완료한다. 단독 검토 장면만 만들고 끝내지 말고 아래 기존 ID를 통해 실제 마을에도 연결한다.
6. **종합 검토:** 현재 마을과 별도 반복 배치 장면에서 검증한 뒤 보고한다. 풀 완성본이 main에 들어오면 자신의 브랜치에 안전하게 반영해 함께 확인한다. 아직 없으면 환경 9종 검토를 끝내고 합본 성능만 미확인으로 남긴다.

작업마다 입력 해시·도구 버전·파라미터·출력 파일을 기록해 중단 후 그 지점부터 재개한다. 오래 걸리는 베이크는 파일 단위로 진행 상태와 로그를 남긴다. 한 에셋이 막혀도 다른 독립 에셋 작업을 완료하고 원인·남은 부분을 구분한다.

## 4. 외형을 지키는 경량화

- 원본을 보관하고 별도 결과를 만든다. 큰 형태와 실루엣은 메시, 작은 돌기·나뭇결·잎맥은 normal/색/roughness 베이크로 옮긴다.
- UV 경계·법선·재질 영역을 보존하는 감면 후보를 비교한다. 새 UV/리토폴로지를 만들면 다시 베이크하며, 원본 텍스처를 새 UV에 그대로 연결하지 않는다. tangent/normal의 좌표계와 색 공간을 확인한다.
- 낮은 측면 마을 카메라와 확대 카메라를 고정해 원본/결과를 같은 조명으로 비교한다. 결과만 밝게 하거나 카메라를 멀리해서 품질 손실을 숨기지 않는다.
- 꿀빛 목재·크림색 돌·세이지 잎·연보라 지붕을 유지한다. 새 원본의 디테일을 예전 단색 primitive 모양으로 되돌리지 않는다.
- 메시/텍스처를 인스턴스끼리 공유하고, 필요한 투명/불투명 재질만 나눈다. 잎·돌·나사 하나마다 Node와 재질을 만들지 않는다.
- 잎 알파 카드는 alpha scissor와 얇은 불투명 메시를 비교하고, mipmap 가장자리·뒷면·그림자·겹침을 확인한다. 양면·투명 처리를 전체 건물에 켜지 않는다.
- 피벗은 바닥 Y=0, 수평 배치 중심을 기준으로 정규화하고 모델 앞 방향·높이를 manifest에 기록한다. 노드 변환을 누락하거나 두 번 적용하지 않는다. 음수 스케일·법선·삼각형 앞뒤도 확인한다.
- 원본이 손상되어 있으면 위치를 보여주고 필요한 부분만 보수한다. 복구 실패를 가리려고 검은 막이나 불투명 물판으로 덮지 않는다.

## 5. 에셋별 필수 주의사항

### 나무 / tree, flower_tree

곧게 올라가는 한 줄기와 간결한 가지를 유지한다. 잎 하나하나 읽히는 외곽과 세이지/황록색 층을 보존한다. 원래 콘셉트의 복잡하게 휜 가지를 다시 만들지 않는다. tree와 flower_tree는 기존 별칭/변형 계약을 유지하고 꽃 변형이 없으면 원본 나무를 공유하되 새로운 승인 디자인이라고 부르지 않는다.

이번에는 정적 품질과 성능을 먼저 완료한다. 잎 바람을 추가한다면 줄기·뿌리는 고정하고 별도 잎 마스크, 바람 0 복원, 변형 AABB를 확인한다. 풀 전용 shader/정점색 채널을 무조건 그대로 적용하지 않는다. 마을 뒤쪽 나무가 새 모델과 중복 표시되지 않게 한다.

### 우물 / well — 투명 물 수정본 보존

현재 업로드된 `stone well.glb`의 해시 `59021fc14bd010da778fee9ab03e6a901852a728e239a9cc3df800ceb1ddedaf`는 대화에서 제공한 **수정 우물**과 일치한다. 원본의 불투명 파란 물로 돌아가지 않는다.

확인된 수정본: 불투명 돌·나무·밧줄 / `Well_Water_Clear` / 수면 아래 내벽이 분리되어 있고, 물 alpha 0.32, roughness 0.095, metallic 0, IOR 1.333, 잔물결 normal이 있다. 별도 내벽은 480 triangles. 현재 물 표면 자체가 65,092 triangles이므로 그대로 게임에 넣지 말고, 물판을 수백~수천 면 수준으로 단순화하며 잔물결 normal 또는 가벼운 shader로 표시를 유지한다. 고정 잔물결과 시간에 따라 움직이는 애니메이션을 혼동하지 않는다.

- 돌/나무는 불투명, 물만 반투명으로 유지하고 수면 아래 내벽·바닥으로 깊이가 보이게 한다.
- 물 위에 기존 파란 불투명 판을 겹쳐 깔지 않는다. 마을의 다른 물 표시 코드와 중복 수면·z-fighting을 검사한다.
- glTF 확장 지원만 믿지 말고 현재 Godot Compatibility에서 실제 투명도·normal·환경 반사가 보이는지 확인한다. 지원하지 않는 고급 굴절을 완료 조건으로 삼지 않는다.
- 가림/공사/설치 미리보기 후 물 재질과 내벽 색이 정확히 복원되어야 한다. 이번 전체 모델 감면 이후 재검증한다.
- 기존 수정본의 원재질에는 명시적 tangent가 없어 검사 경고가 있었다. 재수출한 결과의 tangent 공간과 normal을 확인한다.

### 온실 / greenhouse

기존 배경 온실 표시로 연결한다. 새 농업 시스템을 만들지 않는다. 유리와 프레임을 구분하고, 반투명 유리를 쓸 경우 내부의 비어 있는 면·겹친 유리·잘못된 그리기 순서가 보이지 않는지 확인한다. 돌/나무 프레임까지 투명하게 만들지 않는다. 원본의 흐린 유리 질감을 보존할 수 있으며 내부가 미완성인데 맑은 유리로 무조건 바꾸지 않는다.

### 울타리 / fence, 표지판 / cloud_sign

울타리 한 구간을 이어 배치해 간격·기둥 중복·땅 접촉을 확인한다. 나뭇결은 남기되 막대가 접히거나 구멍이 막히지 않게 한다. 표지판의 구름/문양과 앞 방향을 보존한다. 새 건설 메뉴나 충돌벽을 임의로 추가하지 않는다.

### 창고 / shed, 작업대 / lumber·quarry, 바위 / rock

`warehouse.glb`는 기존 `shed` 표시, `wood workbench.glb`는 `lumber`, `stone workbench.glb`는 `quarry`로 연결한다. 이름만 바꿔 새 시설 정의를 만들지 않는다. 작업자 위치·입구·기존 보급 강화와 생산 효과를 유지한다. 바위는 원본을 공유하고 배치 변형을 쓰되 동일 바위를 새 디자인으로 다시 생성할 필요는 없다.

아직 없는 주택·밭/작물·물레·보·가로등 등은 기존 표시를 유지한다. 뒤에 새 원본이 업로드되면 입력 목록을 갱신해 같은 절차로 확장한다. 물레가 들어오면 회전축과 고정부를 분리하지만, 현재 9종 범위에 없는 모델 제작을 새로 시작하지 않는다.

## 6. 실제 코드 연결 — 재질 소실과 가림 처리

현재 코드에서 확인한 중요한 접점:

| 코드 | 현재 가정 | 이번 변경 시 필요한 대응 |
|---|---|---|
| `GardenAssets.make()` | MeshInstance3D 반환, 단색 vertex-color material_override | 가져온 모델의 UV/텍스처/복수 재질을 보존하는 어댑터 경로 |
| `VillageStage3D._art(), _fit_art()` | MeshInstance3D, mesh AABB, X/Z 압축 배치 | 반환 계약·정규화 높이·원점·선택 범위를 함께 유지 |
| `VillageStage3D._apply_fade()` | 하나의 base_material을 단색 재질로 교체 | 표면별 원재질과 투명도/색을 복원하는 처리 |
| `run_garden_art_tests.gd` | 표면 1개, vertex color, alpha 0.3 고정 가정 | 복수 표면과 원본 alpha가 있는 모델을 실제로 검증하도록 수정 |

권장: `game/assets/environment/objects/`에 모델별 결과와 어댑터를 두고 기존 팩토리에서 9종만 해당 경로로 연결한다. 정적 모델은 여러 재질 표면을 가진 하나의 MeshInstance3D로 정리하면 현재 호출부를 적게 바꿀 수 있다. 여러 노드를 유지한다면 AABB·선택·회전·가림·시험 코드까지 전체 호출자를 함께 대응한다. 어떤 경로든 통째로 단색 material_override를 씌워 새 텍스처와 물을 없애지 않는다.

- 공유 mesh/texture는 보존하고 인스턴스별 상태 변경에는 원본을 훼손하지 않는 재질 복제/파라미터를 사용한다. 매 프레임 복제하지 않는다.
- 유효 alpha는 **원재질 alpha × 공사/미리보기 배율 × 가림 배율**로 계산한다. 같은 값을 반복 곱해 영구적으로 사라지지 않게 한다. 물 alpha를 1로 덮거나 모든 surface를 동일 alpha로 만들지 않는다.
- 유효/무효 배치 tint는 텍스처와 원색 위에 적용하고 복원한다. 우물 2개 중 하나만 가릴 때 다른 우물의 물/돌이 바뀌지 않는지 확인한다.
- 원재질이 StandardMaterial3D인지 ShaderMaterial인지 구분하고 shader의 상태도 복원한다. 물·유리·잎 마스크를 같은 단색 재질로 치환하지 않는다.
- `CELL_W=2.0`, `CELL_D=0.9`와 논리 footprint·회전 규칙을 유지한다. 기존 _fit_art는 Y 높이를 자동으로 맞추지 않으므로 먼저 표시용 높이를 정규화한다. `KIND_HEIGHT`와 실제 선택 영역이 모델을 덮는지 검증한다.
- 입력 미리보기·완공/공사·실제 마을 배경 모두 같은 새 리소스를 사용하게 하고 이전 모델과 중복 생성하지 않는다.
- 현재 미업로드 ID는 기존 factory 경로를 유지한다. 업로드 대상의 결과 누락을 조용히 옛 primitive로 숨기면서 완료 처리하지 않는다.
- 기존 원본 정령과 NPC 애니메이션·저장 데이터는 수정 대상이 아니다.

## 7. 풀 작업과 파일 소유 및 합치기

| 영역 | 담당 |
|---|---|
| `game/assets/environment/grass/`, `tools/environment/grass/`, 풀 검토 장면/보고 | 기존 풀 담당 |
| `game/assets/environment/objects/`, `tools/environment/objects/`, 환경 전용 검토/보고 | 이번 환경 담당 |
| `garden_assets.gd`, `village_stage_3d.gd`, 공통 마을 시험 | 충돌 가능 공유 지점: 최소 수정하고 보고에 변경 함수 명시 |
| `game/project.godot`, 공통 조명·카메라·전역 입력 | 시각 개선을 이유로 일괄 변경하지 않음 |

독립 worktree에서 각각 작업하되 공통 코드 변경은 원격 차이를 확인해 합친다. 풀 작업 파일의 미커밋 상태를 여기서 완료/미완료로 단정하지 않는다. 먼저 준비된 에셋의 검증을 마치고, 통합 시 풀 표시와 새 오브젝트 양쪽을 보존한다. 5개 원본이 두 브랜치에 중복 추가되면 SHA-256이 같을 때 같은 원본 하나로 유지하고 이력이 다른 새 모델이면 내용을 비교한다. 양쪽 브랜치를 자동 merge/force push하지 않는다.

## 8. 실제 검수와 완료 기준

### 파일과 재질
- 9종 입력 실물 해시 확인, 결과 개별 크기·면수·텍스처·표면 수·LOD/피벗 기록.
- glTF 구조 검사와 Godot 실제 import. 새 오류는 해결하고 경고는 영향/처리를 기록.
- 같은 카메라/조명에서 9종 원본/결과 비교, 나무 잎·돌/목재 표면·표지 문양·온실 프레임 확대.
- 우물의 수면 아래 깊이와 물 반사, 두 우물의 독립 가림/복원, 공사→완공/설치미리보기 투명도 확인.

### 마을과 성능
- 마을 바로 진입, 각 대상의 실제 배경/시설 표시, 회전 가능한 시설 모든 방향, 클릭 배치·입구·작업자 위치·선택·가림·저장/재진입.
- 관련 기존 `run_garden_art_tests.gd`, `run_village_entry_tests.gd`, `run_village_tests.gd`를 사용한다. 낡은 단일 표면 검사를 삭제하는 대신 복수 표면·텍스처 유지·원래 alpha 복원·인스턴스 독립성 검사로 바꾼다. triangle count는 모든 표면/노드를 합산한다.
- 실제 마을과 고정 검토 배치 **나무 16 + 바위 16 + 울타리 32 + 표지판 4 + 우물 2 + 창고 2 + 온실 1 + 작업대 각 2**에서 비용을 확인한다. 검토 배치는 리뷰 전용으로 만들고 새 게임 시설을 강제로 배치하지 않는다.
- Godot 버전·renderer·GPU·해상도·VSync·카메라·그림자·보이는 개수·워밍업을 고정해 프레임 시간/FPS·draw calls·전체 삼각형·가능한 메모리를 기록한다. 60FPS는 초기 목표다. 같은 Mac에서 비교하고, 다른 에이전트의 베이크/렌더가 동시에 돌지 않을 때 측정한다.
- GPU가 없는 원격 환경에서도 형식·화면·draw-call 작업량 비교는 참고가 된다. llvmpipe의 FPS를 Mac 하드웨어 성능으로 보고하지 않는다. 실제 GPU 장치 로그가 없으면 하드웨어 성능은 미확인이다.
- 풀 합본이 아직 없으면 환경만의 측정과 합본 미확인을 분리한다. grass 192개 시험은 기존 풀 담당의 범위이며 여기서 다시 맡아 결과를 덮지 않는다.
- 정적 이미지가 시간에 따른 물/바람 동작을 증명하지는 않는다. 실제 동작을 추가했다면 5–10초 엔진 영상과 실행법을 남긴다.

### 제출물
1. 9종 최종 GLB/텍스처·필요 LOD·재질·게임 연결. 원본과 제작 중간 파일은 `game/` 밖.
2. `tools/environment/objects/` 재현 스크립트/설정, `game/assets/environment/objects/manifest.json`의 입력→출력 대응.
3. 실행 가능한 `game/reviews/environment_objects/` 검토 장면, 전후 비교와 실제 마을 스크린샷. 이미지 생성 결과를 엔진 캡처라고 쓰지 않는다.
4. `reports/HWR-ENV-001.md`: 9종 각각 원본 확인/변환/게임 연결/검수 상태, 실제 실행 명령, 기준 커밋, 소스 해시, 성능, 남은 한계와 공유 코드 수정 함수.
5. LFS로 원본 및 해당 규칙의 GLB를 보존하고 결과만 런타임에서 참조한다. 재생성 가능한 대형 임시 배열·베이크 cache를 추가 커밋하지 않는다. 이번 작업의 새 중간 출력은 자기 폴더에서만 제외 처리한다.
6. main의 최신 변경과 풀 작업을 확인한 후 작업 브랜치에 커밋/게시하고 결과 링크를 남긴다. 최종 기준은 원안의 디테일과 실제 마을 동작이 함께 유지되는지다.

## 참고
- [Godot 재질과 투명도](https://docs.godotengine.org/en/stable/tutorials/3d/standard_material_3d.html)
- [Godot 3D 가져오기](https://docs.godotengine.org/en/stable/tutorials/assets_pipeline/importing_3d_scenes/available_formats.html)
- [Git worktree](https://git-scm.com/docs/git-worktree)
